faster-whisper==1.0.2
# ctranslate2 is pulled in by faster-whisper when a compatible wheel exists.
# Installing it explicitly forces a source build on slim containers and stalls
# Railway deploys, so we intentionally leave it out here.
onnxruntime       # optional, not required by faster-whisper
numpy
soundfile
pydub
ffmpeg-python
webrtcvad
fastapi
python-multipart
uvicorn[standard]
